@model CheckBookModelEn

@{
    ViewData["Title"] = "Home Page";
}

<div class="row justify-content-center mt-4">
    <div class="col-md-6 text-center">
        <a href="@Url.Action("Index", "CheckBook", new { language = "pt" })" class="btn btn-link">Português</a> |
        <a href="@Url.Action("Index", "CheckBook", new { language = "en" })" class="btn btn-link">English</a>
    </div>
</div>
